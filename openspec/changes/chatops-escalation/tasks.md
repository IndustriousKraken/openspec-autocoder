## 1. Slack Comms Manager

- [ ] 1.1 Create `src/comms.rs` module. Add `reqwest` and `tokio` (if not already fully imported) dependencies if necessary for HTTP requests.
- [ ] 1.2 Implement `post_question_to_slack(question: &str, config: &config::SlackConfig) -> Result<String>` which sends a message via the Slack Web API and returns the resulting `thread_ts`.
- [ ] 1.3 Implement `poll_slack_thread(thread_ts: &str, config: &config::SlackConfig) -> Result<Option<String>>` which checks if a human has replied to the specific thread.

## 2. In-Memory Escalation Loop

- [ ] 2.1 Modify `src/llm_client.rs` to intercept `ask_user` tool calls generated by the LLM.
- [ ] 2.2 When intercepted, pause the execution loop and invoke `comms::post_question_to_slack`.
- [ ] 2.3 Implement an asynchronous wait loop inside `llm_client.rs` that polls `comms::poll_slack_thread`.
- [ ] 2.4 When an answer is received, inject it back into the LLM conversation history and resume the internal execution loop.

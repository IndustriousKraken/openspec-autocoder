## 1. Commit Architecture Specs

- [ ] 1.1 Copy all capability specs generated in `openspec/changes/orchestrator-architecture/specs` to the root `openspec/specs` directory.
- [ ] 1.2 Verify that all new capabilities have been properly recorded in the main `openspec/specs` directory.
